# design/references/

The three reference screenshots — room selection, room view, patient page.

These are **design references only**. They are recreated as real React/CSS
components; they are never displayed as application screens. They live outside
`public/` on purpose so they are not served by the app or shipped in the bundle.

Any filenames are fine — they'll be listed and mapped explicitly before build.
